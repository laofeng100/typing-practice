# 数据库路径（SQLite）
DATABASE_URL=file:/app/db/custom.db

# Next.js
PORT=3000
HOSTNAME=0.0.0.0
NODE_ENV=production

# 会话签名密钥（生产必设：openssl rand -hex 32 生成；不设则每次重启所有会话失效）
SESSION_SECRET=

# TTS语音服务（可选，不配置则语音功能不可用，练习不受影响）
# TTS_SERVER_URL=http://139.155.115.250
# TTS_TOKEN=listen-eng-2025-secret-key
